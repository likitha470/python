#polymorphisn
#when the same operator it allowed to have diffrent meaning according to context
#operator overloading
print(1+2)#add
print("likitha"+"rai")#cocatinate
print([1,2,3]+[1,2,3])#merge

#DUNDER FUNCTIONS
# a+b (addition)-----------> a__add__(b)
# a-b (subtraction)--------> a__sub__(b)
# a*b (multiplication)-----> a__mul__(b)
# a/b (division)-----------> a__truediv__(b)
# a%b (mod)----------------> a__mod__(b)
